export enum stateJobEnum {
  PENDING = "pending",
  RUNNING = "running",
  FINISHED = "finished",
  DELETED = "deleted",
}

export enum priorityEnum {
  LOWEST = 0,
  LOW = 1,
  NORMAL = 2,
  HIGH = 3,
  HIGHEST = 4,
}
